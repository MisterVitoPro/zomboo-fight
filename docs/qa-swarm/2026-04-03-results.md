# QA Swarm Implementation Results
**Date:** 2026-04-03
**Source spec:** docs/qa-swarm/2026-04-03-spec.md

## Summary
- Fixed: 41/41 issues (all findings addressed)
- Unresolved: 0 issues
- Halted: 0 P0s
- Skipped: 0 issues

## Test Results
- Total tests: 105
- Passing: 105
- Failing: 0

## Fixed Issues

### [P0-005] donePlaying referenced before assignment in menu()
**Priority:** P0
**Attempts:** 1
**Fix applied:** Added `donePlaying = True` before the menu while loop so the variable has a default value if no event sets it.
**Files modified:** `gameLib/main.py`

### [P0-004] Menu reloads background image from disk every frame
**Priority:** P0
**Attempts:** 1
**Fix applied:** Removed `background = pygame.image.load(dataFiles.mainMenuIm)` from inside the menu loop. The image was already loaded before the loop.
**Files modified:** `gameLib/main.py`

### [P0-001] player2="none" string passed to Zombie.update() causes crash
**Priority:** P0
**Attempts:** 1
**Fix applied:** Changed `self.player2 = "none"` to `self.player2 = None`. Added `player2 is not None` guard in Zombie.update() before the jCount==2 branch.
**Files modified:** `gameLib/main.py`, `gameLib/sprites.py`

### [P0-002] Spawn rate min/max go negative, causing randrange crash
**Priority:** P0
**Attempts:** 1
**Fix applied:** Changed `zombieSMin -= 6` to `zombieSMin = max(zombieSMin - 6, 5)` and `zombieSMax -= 6` to `zombieSMax = max(zombieSMax - 6, 10)`.
**Files modified:** `gameLib/main.py`

### [P0-003] BigZombie can never spawn -- unreachable code
**Priority:** P0
**Attempts:** 1
**Fix applied:** Changed `randrange(1, 12)` to `randrange(1, 13)` and `elif zNum > 13` to `else` so BigZombie spawns ~8% of the time.
**Files modified:** `gameLib/main.py`

### [P1-001] Sprite groups never cleared between games
**Priority:** P1
**Attempts:** 1
**Fix applied:** Added loop to `.empty()` all sprite groups in `sGroups` before `makeSprites()` in `game.__init__()`.
**Files modified:** `gameLib/main.py`

### [P1-002] Module-level joystick/mixer init runs at import time
**Priority:** P1
**Attempts:** 1
**Fix applied:** Wrapped `pygame.joystick.init()` and `pygame.mixer.init()` in try/except blocks. Added `j = None` and `j2 = None` defaults so code doesn't crash on NameError when no joystick is present.
**Files modified:** `gameLib/sprites.py`

### [P1-003] Bare except in Levels.get_at() silently swallows all errors
**Priority:** P1
**Attempts:** 1
**Fix applied:** Changed `except:` to `except (IndexError, TypeError):` to only catch expected pixel access errors.
**Files modified:** `gameLib/Levels.py`

### [P1-004] Zombie accesses player.oldxy2 which may not be initialized
**Priority:** P1
**Attempts:** 1
**Fix applied:** Removed the `timerPos >= 180` gate on `self.oldxy2 = self.oldxy` so it updates every frame.
**Files modified:** `gameLib/player.py`

### [P1-005] Turret.calcVector crashes when jOn is False
**Priority:** P1
**Attempts:** 1
**Fix applied:** Changed `self.dir = 0` to `self.dir = (0, -1)` in Turret.__init__() so calcVector() can subscript it.
**Files modified:** `gameLib/sprites.py`

### [P1-006] No bounds checking on health bar scale
**Priority:** P1
**Attempts:** 1
**Fix applied:** Added `width = max(self.player.hp, 1)` before `pygame.transform.scale()` to prevent zero-width surfaces.
**Files modified:** `gameLib/UI_HUD.py`

### [P1-007] Player.fire() with dir=(0,0) creates stationary bullets
**Priority:** P1
**Attempts:** 1
**Fix applied:** Changed default `self.dir` from `(0, 0)` to `(0, -1)` in Player.__init__(). Added early return in `fire()` when dir==(0,0).
**Files modified:** `gameLib/player.py`

### [P1-008] Shotgun spread calculation uses wrong axes
**Priority:** P1
**Attempts:** 1
**Fix applied:** Replaced manual axis-based shotgun spread with angle-based math using `math.atan2`, `math.cos`, `math.sin` with a spread angle of 0.15 radians. Now produces 3 distinct bullets in all 8 directions.
**Files modified:** `gameLib/player.py`

### [P1-009] Zombie collision with bullet calls on_collide twice
**Priority:** P1
**Attempts:** 1
**Fix applied:** Changed `collider.on_collide(collider)` to `collider.on_collide(self)` so the bullet receives the zombie, not itself.
**Files modified:** `gameLib/sprites.py`

### [P1-010] Pickup.on_collide() signature mismatch with subclasses
**Priority:** P1
**Attempts:** 1
**Fix applied:** Changed base `Pickup.on_collide(self)` to `Pickup.on_collide(self, player)` so it matches subclass signatures.
**Files modified:** `gameLib/pickups.py`

### [P1-011] Wall collision applies damage to player
**Priority:** P1
**Attempts:** 1
**Fix applied:** Changed wall collision to call `self.wallCollide(collider)` instead of `self.on_collide(collider)`. Added new `wallCollide` method that does push-apart only, no damage.
**Files modified:** `gameLib/player.py`

### [P1-012] Flamethrower reload gives only 100 ammo but threshold is 300
**Priority:** P1
**Attempts:** 1
**Fix applied:** Changed `self.ammo < 300` to `self.ammo < 100` in both joystick and keyboard reload paths.
**Files modified:** `gameLib/player.py`

### [P1-013] Game over state has no actual game-ending logic
**Priority:** P1
**Attempts:** 1
**Fix applied:** Added `gameOverTimer` in mainLoop that increments when `player1.dead` is True and sets `keepGoing = False` after 9*30 frames.
**Files modified:** `gameLib/main.py`

### [P1-014] Grenade deceleration is frame-dependent
**Priority:** P1
**Attempts:** 1
**Fix applied:** Added `dt=1.0/30.0` parameter to Grenade.update() with `decel = 3.0 * dt` so deceleration scales with frame time.
**Files modified:** `gameLib/sprites.py`

### [P1-015] zombieSpawner position randomized every frame
**Priority:** P1
**Attempts:** 1
**Fix applied:** Moved `centerRects()` calls from `update()` to `__init__()` so position is set once at creation.
**Files modified:** `gameLib/zombieSpawn.py`

### [P1-016] Level system is disabled but still imported
**Priority:** P1
**Attempts:** 1
**Fix applied:** Removed `Levels` import from main.py.
**Files modified:** `gameLib/main.py`

### [P2-001] No sound error handling -- missing audio files crash the game
**Priority:** P2
**Attempts:** 1
**Fix applied:** Added `load_sound()` helper and `_SilentSound` fallback class to dataFiles.py. Changed all `pygame.mixer.Sound(...)` calls in player.py and pickups.py to use `dataFiles.load_sound(...)`.
**Files modified:** `gameLib/dataFiles.py`, `gameLib/player.py`, `gameLib/pickups.py`

### [P2-002] Asset paths use relative "../data/" which breaks if CWD changes
**Priority:** P2
**Attempts:** 1
**Fix applied:** Changed dataFiles.py to compute paths relative to the module's `__file__` location using `os.path.dirname(os.path.abspath(__file__))`.
**Files modified:** `gameLib/dataFiles.py`

### [P2-005] No collision between zombies -- they stack on top of each other
**Priority:** P2
**Attempts:** 1
**Fix applied:** Added zombie-zombie collision push-apart logic in Zombie.update() that checks `sGroups.zombieSprites` and nudges overlapping zombies apart.
**Files modified:** `gameLib/sprites.py`

### [P2-006] Explosion damage applies every frame for 5 frames
**Priority:** P2
**Attempts:** 1
**Fix applied:** Added `self.damaged = set()` to Explosion.__init__() and check `id(self) not in collider.damaged` before applying damage in Zombie.update().
**Files modified:** `gameLib/sprites.py`

### [P2-007] LaserSight hardcodes image path instead of using dataFiles
**Priority:** P2
**Attempts:** 1
**Fix applied:** Changed `pygame.image.load("../data/Laser_Pointer.tga")` to `pygame.image.load(dataFiles.laserSightIm)`.
**Files modified:** `gameLib/sprites.py`

### [P2-010] UIsystem.py is entirely unused
**Priority:** P2
**Attempts:** 1
**Fix applied:** Deleted `gameLib/UIsystem.py`.
**Files modified:** Deleted `gameLib/UIsystem.py`

### [P2-011] VECTOR.py and rotate.py are unused
**Priority:** P2
**Attempts:** 1
**Fix applied:** Deleted `gameLib/VECTOR.py` and `gameLib/rotate.py`.
**Files modified:** Deleted `gameLib/VECTOR.py`, `gameLib/rotate.py`

### [P2-012] .pyc files tracked/present, no .gitignore
**Priority:** P2
**Attempts:** 1
**Fix applied:** Created `.gitignore` with `__pycache__/`, `*.pyc`, and other standard Python ignores.
**Files modified:** Created `.gitignore`

### [P3-001] Python 2 print statements in comments
**Priority:** P3
**Attempts:** 2
**Fix applied:** Removed all commented-out Python 2 print statements from sprites.py and player.py (including 4 in on_collide method).
**Files modified:** `gameLib/sprites.py`, `gameLib/player.py`

### [P3-005] No .gitignore file
**Priority:** P3
**Attempts:** 1
**Fix applied:** Addressed together with P2-012 above.
**Files modified:** Created `.gitignore`

### [P3-006] Local variable shadows 'dir' built-in
**Priority:** P3
**Attempts:** 1
**Fix applied:** Renamed all local `dir = "west"` etc. to `direction = "west"` in gameObject.loadImage().
**Files modified:** `gameLib/sprites.py`

## Unresolved Issues
None.

## Halted Issues
None.

## Round 2 Fixes (Previously Skipped as Invasive/Risky)

### [P2-003] Player imageChange() has identical copy-pasted logic
**Priority:** P2
**Attempts:** 1
**Fix applied:** Removed the duplicate `if self.pNum == 1` / `if self.pNum == 2` branches since both had identical logic. Now a single unconditional block.
**Files modified:** `gameLib/player.py`

### [P2-004] Magic numbers throughout codebase
**Priority:** P2
**Attempts:** 1
**Fix applied:** Added `SCREEN_WIDTH`, `SCREEN_HEIGHT`, `FPS` constants to dataFiles.py. Replaced all hardcoded 800, 600, 30 references across main.py, player.py, sprites.py, pickups.py, zombieSpawn.py.
**Files modified:** `gameLib/dataFiles.py`, `gameLib/main.py`, `gameLib/player.py`, `gameLib/sprites.py`, `gameLib/pickups.py`, `gameLib/zombieSpawn.py`

### [P2-008] Pickup rotation creates new surface every frame
**Priority:** P2
**Attempts:** 1
**Fix applied:** Added `_rotation_cache` class-level dict to Pickup. Precomputes all 181 rotation frames in `__init__` and indexes them by angle in `update()` instead of calling `pygame.transform.rotate()` each frame.
**Files modified:** `gameLib/pickups.py`

### [P2-009] No frame-rate independence
**Priority:** P2
**Attempts:** 1
**Fix applied:** Computed normalized `dt` from `clock.tick()` in main_loop (dt=1.0 at target FPS). Passed dt through updateGroups to Player.update, Zombie.update, Bullet.update, and Grenade.update. All movement now scales by `* dt`. Grenade deceleration formula updated to match new dt convention.
**Files modified:** `gameLib/main.py`, `gameLib/player.py`, `gameLib/sprites.py`

### [P2-013] sGroups uses mutable module-level globals
**Priority:** P2
**Attempts:** 1
**Fix applied:** Created `SpriteGroups` class with `GROUP_NAMES` tuple and `empty_all()` method. Module-level attributes backed by a single `_instance`. Main.py now uses `sGroups.empty_all()` instead of manual loop.
**Files modified:** `gameLib/sGroups.py`, `gameLib/main.py`

### [P2-014] animate.py class-level mutable dicts
**Priority:** P2
**Attempts:** 1
**Fix applied:** Wrapped `offsets` and `imageSizes` dicts in `types.MappingProxyType` to make them read-only at runtime.
**Files modified:** `gameLib/animate.py`

### [P3-002] Inconsistent naming conventions
**Priority:** P3
**Attempts:** 1
**Fix applied:** Bulk-renamed 18 camelCase methods to snake_case (e.g., `loadImage` -> `load_image`, `spawnZombie` -> `spawn_zombie`) and 8 non-PascalCase classes to PascalCase (e.g., `gameObject` -> `GameObject`, `shotgunPickup` -> `ShotgunPickup`). All cross-file references and tests updated.
**Files modified:** All gameLib/*.py and tests/*.py files

### [P3-003] Missing docstrings
**Priority:** P3
**Attempts:** 1
**Fix applied:** Added one-line docstrings to all 20+ classes and key public methods across sprites.py, player.py, pickups.py, UI_HUD.py, Levels.py, zombieSpawn.py, and animate.py.
**Files modified:** `gameLib/sprites.py`, `gameLib/player.py`, `gameLib/pickups.py`, `gameLib/UI_HUD.py`, `gameLib/Levels.py`, `gameLib/zombieSpawn.py`, `gameLib/animate.py`

### [P3-004] Copyright headers reference outdated dates and 'student' author
**Priority:** P3
**Attempts:** 1
**Fix applied:** Updated all 11 gameLib files to use triple-quote docstring module headers with file description, standardized author to "Anthony D'Alessandro", removed "@author: student" references.
**Files modified:** All gameLib/*.py files
